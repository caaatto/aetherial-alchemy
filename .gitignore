# Dependencies
node_modules/

# Build output
dist/

# Backend runtime state (party inventory store — regenerated at runtime)
api/inventory-store.json
api/grants-store.json

# Extension signing keys — must NEVER be committed (lives in ~/.secrets)
*.pem

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
.vscode/
*.swp
